import re

def validate_password(pswd):
    
    # brug regular expression til at validere password
    # mindst 16 karakterer
    # skal indeholde mindst et af hver af følgende:
    #   - udvidet ASCII tegn
    #   - tal
    #   - store bogstaver
    #   - små bogstaver
    validated_password = re.findall()
    return validated_password

def validate_email(email):

    # brug regular expression til at validere email addresse

    validated_email = re.findall()

    return validated_email

def validate_ip_address(ip):

    # brug regular expression til at validere email addresse

    validated_ip = re.findall()

    return validated_ip
